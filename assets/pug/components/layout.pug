block variables

doctype
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")

        link(rel="icon" type="image/png" sizes="32x32" href=require("/assets/images/favicon-32x32.png"))
        link(rel="stylesheet" href=require("/assets/scss/index.scss"))

        title Frontend Mentor | Recipe page

        style=".attribution { font-size: 11px; text-align: center; } .attribution a { color: hsl(228, 45%, 44%); }"

    body
        block content